{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev && npm run build:back"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=finsight-ai-serve",
    "restartPolicyType": "ON_FAILURE"
  }
}