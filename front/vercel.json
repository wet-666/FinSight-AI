{
  "framework": "vite",
  "installCommand": "npm install --prefix=.. --ignore-scripts=false",
  "buildCommand": "npm run build-only",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!api/|uploads/).*)", "destination": "/index.html" }
  ]
}
